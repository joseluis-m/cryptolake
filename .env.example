# ============================================================
# CryptoLake — Environment Variables
# ============================================================
# Copy this file to .env before starting:  cp .env.example .env
# NEVER commit the .env file (it may contain secrets)
# ============================================================

# -- MinIO (S3-compatible storage) --
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=cryptolake
MINIO_SECRET_KEY=cryptolake123

# -- Kafka --
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# -- Iceberg REST Catalog --
ICEBERG_CATALOG_URI=http://localhost:8181

# -- Airflow --
AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_PASSWORD=admin

# -- External APIs --
COINGECKO_BASE_URL=https://api.coingecko.com/api/v3
FEAR_GREED_URL=https://api.alternative.me/fng/

# -- Tracked coins (comma-separated CoinGecko IDs) --
TRACKED_COINS=bitcoin,ethereum,solana,cardano,polkadot,chainlink,avalanche-2,polygon-ecosystem-token
